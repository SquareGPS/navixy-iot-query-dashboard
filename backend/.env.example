# ============================================================================
# Backend Environment Variables (.env)
# ============================================================================
# This file is used when running the backend in DEVELOPMENT MODE:
#   - npm run dev (in backend directory)
#   - npm run dev:backend (from root)
#   - npm run dev:full (from root)
#
# For Docker Compose, use the root .env file instead.
# ============================================================================

# Environment Configuration
NODE_ENV=development
PORT=3001

# Database Configuration (Local PostgreSQL)
# Used for: User authentication, app settings, dashboard schemas
DATABASE_URL=postgresql://danilnezhdanov@localhost:5432/reports_app_db

# Redis Cache (optional - for query result caching)
REDIS_URL=redis://localhost:6379

# JWT Configuration
# REQUIRED: Generate a secure secret for production:
#   openssl rand -hex 32
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=24h

# Security - More reasonable limits for development/testing
RATE_LIMIT_WINDOW_MS=300000
RATE_LIMIT_MAX_REQUESTS=5000

# Analytics Service
ANALYTICS_SERVICE_URL=http://localhost:8001

# Report Schema Repository
# URL to the example dashboard schema used by the "Download Example Schema" feature
REPORT_SCHEMA_URL=https://raw.githubusercontent.com/DanilNezhdanov/navixy-dashboard-schema/refs/heads/main/examples/hello-world-dashboard.json

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log
