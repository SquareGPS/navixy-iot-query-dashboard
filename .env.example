# ============================================================================
# Docker Compose Environment Variables (.env)
# ============================================================================
# This file is used when running services via Docker Compose:
#   - npm run docker:up
#   - docker-compose up
#
# For backend development mode, use backend/.env instead.
# ============================================================================

# JWT Secret for authentication (REQUIRED)
# Generate a secure secret: openssl rand -hex 32
JWT_SECRET=

# Client Settings Database Username (REQUIRED)
# This username is used to connect to the client's database for storing
# user settings (users, user_roles, global_variables, reports, sections).
# The password is taken from the user's login URL (iotDbUrl).
CLIENT_SETTINGS_DB_USER=dashboard_settings

# Report Schema URL (optional - defaults shown in docker-compose.yml)
# URL to the example dashboard schema used by the "Download Example Schema" feature
REPORT_SCHEMA_URL=https://raw.githubusercontent.com/DanilNezhdanov/navixy-dashboard-schema/refs/heads/main/examples/hello-world-dashboard.json
