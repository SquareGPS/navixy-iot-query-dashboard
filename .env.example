# ============================================================================
# Docker Compose Environment Variables (.env)
# ============================================================================
# This file is used when running services via Docker Compose:
#   - npm run docker:up
#   - docker-compose up
#
# For backend development mode, use backend/.env instead.
# ============================================================================

# JWT Secret for authentication (REQUIRED)
# Generate a secure secret: openssl rand -hex 32
JWT_SECRET=

# Database credentials (optional - defaults shown in docker-compose.yml)
POSTGRES_DB=reports_app_db
POSTGRES_USER=reports_user
POSTGRES_PASSWORD=postgres

# Report Schema URL (optional - defaults shown in docker-compose.yml)
# URL to the example dashboard schema used by the "Download Example Schema" feature
REPORT_SCHEMA_URL=https://raw.githubusercontent.com/DanilNezhdanov/navixy-dashboard-schema/refs/heads/main/examples/hello-world-dashboard.json

# Frontend Environment Variables (build-time)
# These are used when building the frontend with: npm run build
# For local development, use .env.local instead (see .env.local.example)

# Metabase Database Connection URL (for login screen prepopulation)
# This prepopulates the Metabase database connection field on the login screen.
# This is the external database used for Metabase queries, separate from the app database.
#
# For Docker/EC2 (connecting to PostgreSQL container):
VITE_DEFAULT_METABASE_DB_CONNECTION_URL=postgresql://reports_user:postgres@postgres:5432/reports_app_db
#
# For local development, see .env.local.example
