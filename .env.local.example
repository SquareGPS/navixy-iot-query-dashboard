# ============================================================================
# Local Development Environment Variables (.env.local)
# ============================================================================
# This file is for local development overrides.
# Copy this file to .env.local and customize for your local setup.
# .env.local is gitignored and will not be committed.
# ============================================================================

# Metabase Database Connection URL (for login screen prepopulation)
# This is the external database used for Metabase queries, separate from the app database.
# 
# IMPORTANT: When running backend in Docker, use host.docker.internal instead of 127.0.0.1
# to connect to services running on your host machine.
#
# Local development (backend in Docker, PostgreSQL on host):
VITE_DEFAULT_METABASE_DB_CONNECTION_URL=postgresql://reports_user@host.docker.internal:5432/reports_app_db

# Alternative: If PostgreSQL is also in Docker, use the Docker service name:
# VITE_DEFAULT_METABASE_DB_CONNECTION_URL=postgresql://reports_user:postgres@postgres:5432/reports_app_db

# Note: Vite environment variables are build-time only.
# You need to rebuild the frontend after changing this value:
#   npm run build
